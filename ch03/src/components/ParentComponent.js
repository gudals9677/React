import React, { Component } from 'react';

export default class ParentComponent extends Component {
    render() {
        return (
            <div>
                ParentComponent
                <h4>부모 컴포넌트</h4>
                <p>부모 컴포넌트 입니다.</p>
            </div>
        );
    }
}
